<%- include("partials/header") %>

<h2>📚 Books</h2>

<form action="/books/add" method="POST" class="form-box">
  <input name="title" placeholder="Book Title" required>
  <input name="author" placeholder="Author" required>
  <input name="quantity" type="number" min="0" placeholder="Quantity" required>
  <button type="submit">➕ Add Book</button>
</form>

<ul class="list">
  <% books.forEach(b => { %>
    <li>
      <div class="book-info">
        <strong><%= b.title %></strong><br>
        by <%= b.author %> (Qty: <%= b.quantity %>)
      </div>

      <div class="actions">
        <a href="/books/edit/<%= b._id %>" class="edit">✏ Edit</a>
        <a href="/books/delete/<%= b._id %>" class="delete"
           onclick="return confirm('Delete book?')">🗑 Delete</a>
      </div>
    </li>
  <% }) %>
</ul>

<%- include("partials/footer") %>
